1)sudo apt update
2)sudo apt install curl -y
3)curl -fsSL https://pgp.mongodb.com/server-6.0.asc | sudo apt-key add -
4)echo "deb [arch=amd64] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
5)sudo apt update
6)sudo apt install -y mongodb-mongosh
7)mongosh --version
